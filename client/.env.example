# Local dev: Vite proxies /api to the server (see vite.config.js).
VITE_API_URL=/api
VITE_SOCKET_URL=http://localhost:5000

# Production split (e.g. Netlify + Render): set both to your API origin.
# VITE_API_URL=https://your-api.onrender.com/api
# VITE_SOCKET_URL=https://your-api.onrender.com
