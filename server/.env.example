PORT=5000
MONGODB_URI=mongodb://localhost:27017/smart-city-map
JWT_SECRET=your-super-secret-jwt-key-change-in-production
CLIENT_URL=http://localhost:5173
# Optional: comma-separated extra browser origins (e.g. a Cloudflare quick tunnel URL).
# Quick tunnels (*.trycloudflare.com) are also accepted automatically when the server allows it.
# CLIENT_URLS=https://xxxx.trycloudflare.com
